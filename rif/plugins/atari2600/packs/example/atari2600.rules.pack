adc_imm:
    need REG, VALUE, SYMBOL, imm
    bitfit imm.binary, 8
    zext _imm8, imm.binary, 8
    emit 01101001
    emit _imm8

adc_zp:
    need REG, VALUE, SYMBOL, addr
    bitfit addr.binary, 8
    zext _addr8, addr.binary, 8
    emit 01100101
    emit _addr8

adc_zpx:
    need REG, VALUE, SYMBOL, addr
    need REG, x_reg
    bitfit addr.binary, 8
    zext _addr8, addr.binary, 8
    emit 01110101
    emit _addr8

adc_abs:
    need LABEL, target
    emit 01101101
    reloc abs, target, 16

adc_abs_addr:
    need REG, VALUE, SYMBOL, addr
    bitfit addr.binary, 16
    zext _addr16, addr.binary, 16
    emit 01101101
    emit _addr16

adc_absx:
    need LABEL, target
    need REG, x_reg
    emit 01111101
    reloc abs, target, 16

adc_absx_addr:
    need REG, VALUE, SYMBOL, addr
    need REG, x_reg
    bitfit addr.binary, 16
    zext _addr16, addr.binary, 16
    emit 01111101
    emit _addr16

adc_absy:
    need LABEL, target
    need REG, y_reg
    emit 01111001
    reloc abs, target, 16

adc_absy_addr:
    need REG, VALUE, SYMBOL, addr
    need REG, y_reg
    bitfit addr.binary, 16
    zext _addr16, addr.binary, 16
    emit 01111001
    emit _addr16

adc_indx:
    need REG, VALUE, SYMBOL, addr
    need REG, x_reg
    bitfit addr.binary, 8
    zext _addr8, addr.binary, 8
    emit 01100001
    emit _addr8

adc_indy:
    need REG, VALUE, SYMBOL, addr
    need REG, y_reg
    bitfit addr.binary, 8
    zext _addr8, addr.binary, 8
    emit 01110001
    emit _addr8

and_imm:
    need REG, VALUE, SYMBOL, imm
    bitfit imm.binary, 8
    zext _imm8, imm.binary, 8
    emit 00101001
    emit _imm8

and_zp:
    need REG, VALUE, SYMBOL, addr
    bitfit addr.binary, 8
    zext _addr8, addr.binary, 8
    emit 00100101
    emit _addr8

and_zpx:
    need REG, VALUE, SYMBOL, addr
    need REG, x_reg
    bitfit addr.binary, 8
    zext _addr8, addr.binary, 8
    emit 00110101
    emit _addr8

and_abs:
    need LABEL, target
    emit 00101101
    reloc abs, target, 16

and_abs_addr:
    need REG, VALUE, SYMBOL, addr
    bitfit addr.binary, 16
    zext _addr16, addr.binary, 16
    emit 00101101
    emit _addr16

and_absx:
    need LABEL, target
    need REG, x_reg
    emit 00111101
    reloc abs, target, 16

and_absx_addr:
    need REG, VALUE, SYMBOL, addr
    need REG, x_reg
    bitfit addr.binary, 16
    zext _addr16, addr.binary, 16
    emit 00111101
    emit _addr16

and_absy:
    need LABEL, target
    need REG, y_reg
    emit 00111001
    reloc abs, target, 16

and_absy_addr:
    need REG, VALUE, SYMBOL, addr
    need REG, y_reg
    bitfit addr.binary, 16
    zext _addr16, addr.binary, 16
    emit 00111001
    emit _addr16

and_indx:
    need REG, VALUE, SYMBOL, addr
    need REG, x_reg
    bitfit addr.binary, 8
    zext _addr8, addr.binary, 8
    emit 00100001
    emit _addr8

and_indy:
    need REG, VALUE, SYMBOL, addr
    need REG, y_reg
    bitfit addr.binary, 8
    zext _addr8, addr.binary, 8
    emit 00110001
    emit _addr8

asl:
    emit 00001010

asl_acc:
    emit 00001010

asl_zp:
    need REG, VALUE, SYMBOL, addr
    bitfit addr.binary, 8
    zext _addr8, addr.binary, 8
    emit 00000110
    emit _addr8

asl_zpx:
    need REG, VALUE, SYMBOL, addr
    need REG, x_reg
    bitfit addr.binary, 8
    zext _addr8, addr.binary, 8
    emit 00010110
    emit _addr8

asl_abs:
    need LABEL, target
    emit 00001110
    reloc abs, target, 16

asl_abs_addr:
    need REG, VALUE, SYMBOL, addr
    bitfit addr.binary, 16
    zext _addr16, addr.binary, 16
    emit 00001110
    emit _addr16

asl_absx:
    need LABEL, target
    need REG, x_reg
    emit 00011110
    reloc abs, target, 16

asl_absx_addr:
    need REG, VALUE, SYMBOL, addr
    need REG, x_reg
    bitfit addr.binary, 16
    zext _addr16, addr.binary, 16
    emit 00011110
    emit _addr16

bcc:
    need LABEL, target
    emit 10010000
    reldis ., target, 8

bcc_rel:
    need LABEL, target
    emit 10010000
    reldis ., target, 8

bcs:
    need LABEL, target
    emit 10110000
    reldis ., target, 8

bcs_rel:
    need LABEL, target
    emit 10110000
    reldis ., target, 8

beq:
    need LABEL, target
    emit 11110000
    reldis ., target, 8

beq_rel:
    need LABEL, target
    emit 11110000
    reldis ., target, 8

bit_zp:
    need REG, VALUE, SYMBOL, addr
    bitfit addr.binary, 8
    zext _addr8, addr.binary, 8
    emit 00100100
    emit _addr8

bit_abs:
    need LABEL, target
    emit 00101100
    reloc abs, target, 16

bit_abs_addr:
    need REG, VALUE, SYMBOL, addr
    bitfit addr.binary, 16
    zext _addr16, addr.binary, 16
    emit 00101100
    emit _addr16

bmi:
    need LABEL, target
    emit 00110000
    reldis ., target, 8

bmi_rel:
    need LABEL, target
    emit 00110000
    reldis ., target, 8

bne:
    need LABEL, target
    emit 11010000
    reldis ., target, 8

bne_rel:
    need LABEL, target
    emit 11010000
    reldis ., target, 8

bpl:
    need LABEL, target
    emit 00010000
    reldis ., target, 8

bpl_rel:
    need LABEL, target
    emit 00010000
    reldis ., target, 8

brk:
    emit 00000000

bvc:
    need LABEL, target
    emit 01010000
    reldis ., target, 8

bvc_rel:
    need LABEL, target
    emit 01010000
    reldis ., target, 8

bvs:
    need LABEL, target
    emit 01110000
    reldis ., target, 8

bvs_rel:
    need LABEL, target
    emit 01110000
    reldis ., target, 8

clc:
    emit 00011000

cld:
    emit 11011000

cli:
    emit 01011000

clv:
    emit 10111000

cmp_imm:
    need REG, VALUE, SYMBOL, imm
    bitfit imm.binary, 8
    zext _imm8, imm.binary, 8
    emit 11001001
    emit _imm8

cmp_zp:
    need REG, VALUE, SYMBOL, addr
    bitfit addr.binary, 8
    zext _addr8, addr.binary, 8
    emit 11000101
    emit _addr8

cmp_zpx:
    need REG, VALUE, SYMBOL, addr
    need REG, x_reg
    bitfit addr.binary, 8
    zext _addr8, addr.binary, 8
    emit 11010101
    emit _addr8

cmp_abs:
    need LABEL, target
    emit 11001101
    reloc abs, target, 16

cmp_abs_addr:
    need REG, VALUE, SYMBOL, addr
    bitfit addr.binary, 16
    zext _addr16, addr.binary, 16
    emit 11001101
    emit _addr16

cmp_absx:
    need LABEL, target
    need REG, x_reg
    emit 11011101
    reloc abs, target, 16

cmp_absx_addr:
    need REG, VALUE, SYMBOL, addr
    need REG, x_reg
    bitfit addr.binary, 16
    zext _addr16, addr.binary, 16
    emit 11011101
    emit _addr16

cmp_absy:
    need LABEL, target
    need REG, y_reg
    emit 11011001
    reloc abs, target, 16

cmp_absy_addr:
    need REG, VALUE, SYMBOL, addr
    need REG, y_reg
    bitfit addr.binary, 16
    zext _addr16, addr.binary, 16
    emit 11011001
    emit _addr16

cmp_indx:
    need REG, VALUE, SYMBOL, addr
    need REG, x_reg
    bitfit addr.binary, 8
    zext _addr8, addr.binary, 8
    emit 11000001
    emit _addr8

cmp_indy:
    need REG, VALUE, SYMBOL, addr
    need REG, y_reg
    bitfit addr.binary, 8
    zext _addr8, addr.binary, 8
    emit 11010001
    emit _addr8

cpx_imm:
    need REG, VALUE, SYMBOL, imm
    bitfit imm.binary, 8
    zext _imm8, imm.binary, 8
    emit 11100000
    emit _imm8

cpx_zp:
    need REG, VALUE, SYMBOL, addr
    bitfit addr.binary, 8
    zext _addr8, addr.binary, 8
    emit 11100100
    emit _addr8

cpx_abs:
    need LABEL, target
    emit 11101100
    reloc abs, target, 16

cpx_abs_addr:
    need REG, VALUE, SYMBOL, addr
    bitfit addr.binary, 16
    zext _addr16, addr.binary, 16
    emit 11101100
    emit _addr16

cpy_imm:
    need REG, VALUE, SYMBOL, imm
    bitfit imm.binary, 8
    zext _imm8, imm.binary, 8
    emit 11000000
    emit _imm8

cpy_zp:
    need REG, VALUE, SYMBOL, addr
    bitfit addr.binary, 8
    zext _addr8, addr.binary, 8
    emit 11000100
    emit _addr8

cpy_abs:
    need LABEL, target
    emit 11001100
    reloc abs, target, 16

cpy_abs_addr:
    need REG, VALUE, SYMBOL, addr
    bitfit addr.binary, 16
    zext _addr16, addr.binary, 16
    emit 11001100
    emit _addr16

dec_zp:
    need REG, VALUE, SYMBOL, addr
    bitfit addr.binary, 8
    zext _addr8, addr.binary, 8
    emit 11000110
    emit _addr8

dec_zpx:
    need REG, VALUE, SYMBOL, addr
    need REG, x_reg
    bitfit addr.binary, 8
    zext _addr8, addr.binary, 8
    emit 11010110
    emit _addr8

dec_abs:
    need LABEL, target
    emit 11001110
    reloc abs, target, 16

dec_abs_addr:
    need REG, VALUE, SYMBOL, addr
    bitfit addr.binary, 16
    zext _addr16, addr.binary, 16
    emit 11001110
    emit _addr16

dec_absx:
    need LABEL, target
    need REG, x_reg
    emit 11011110
    reloc abs, target, 16

dec_absx_addr:
    need REG, VALUE, SYMBOL, addr
    need REG, x_reg
    bitfit addr.binary, 16
    zext _addr16, addr.binary, 16
    emit 11011110
    emit _addr16

dex:
    emit 11001010

dey:
    emit 10001000

eor_imm:
    need REG, VALUE, SYMBOL, imm
    bitfit imm.binary, 8
    zext _imm8, imm.binary, 8
    emit 01001001
    emit _imm8

eor_zp:
    need REG, VALUE, SYMBOL, addr
    bitfit addr.binary, 8
    zext _addr8, addr.binary, 8
    emit 01000101
    emit _addr8

eor_zpx:
    need REG, VALUE, SYMBOL, addr
    need REG, x_reg
    bitfit addr.binary, 8
    zext _addr8, addr.binary, 8
    emit 01010101
    emit _addr8

eor_abs:
    need LABEL, target
    emit 01001101
    reloc abs, target, 16

eor_abs_addr:
    need REG, VALUE, SYMBOL, addr
    bitfit addr.binary, 16
    zext _addr16, addr.binary, 16
    emit 01001101
    emit _addr16

eor_absx:
    need LABEL, target
    need REG, x_reg
    emit 01011101
    reloc abs, target, 16

eor_absx_addr:
    need REG, VALUE, SYMBOL, addr
    need REG, x_reg
    bitfit addr.binary, 16
    zext _addr16, addr.binary, 16
    emit 01011101
    emit _addr16

eor_absy:
    need LABEL, target
    need REG, y_reg
    emit 01011001
    reloc abs, target, 16

eor_absy_addr:
    need REG, VALUE, SYMBOL, addr
    need REG, y_reg
    bitfit addr.binary, 16
    zext _addr16, addr.binary, 16
    emit 01011001
    emit _addr16

eor_indx:
    need REG, VALUE, SYMBOL, addr
    need REG, x_reg
    bitfit addr.binary, 8
    zext _addr8, addr.binary, 8
    emit 01000001
    emit _addr8

eor_indy:
    need REG, VALUE, SYMBOL, addr
    need REG, y_reg
    bitfit addr.binary, 8
    zext _addr8, addr.binary, 8
    emit 01010001
    emit _addr8

inc_zp:
    need REG, VALUE, SYMBOL, addr
    bitfit addr.binary, 8
    zext _addr8, addr.binary, 8
    emit 11100110
    emit _addr8

inc_zpx:
    need REG, VALUE, SYMBOL, addr
    need REG, x_reg
    bitfit addr.binary, 8
    zext _addr8, addr.binary, 8
    emit 11110110
    emit _addr8

inc_abs:
    need LABEL, target
    emit 11101110
    reloc abs, target, 16

inc_abs_addr:
    need REG, VALUE, SYMBOL, addr
    bitfit addr.binary, 16
    zext _addr16, addr.binary, 16
    emit 11101110
    emit _addr16

inc_absx:
    need LABEL, target
    need REG, x_reg
    emit 11111110
    reloc abs, target, 16

inc_absx_addr:
    need REG, VALUE, SYMBOL, addr
    need REG, x_reg
    bitfit addr.binary, 16
    zext _addr16, addr.binary, 16
    emit 11111110
    emit _addr16

inx:
    emit 11101000

iny:
    emit 11001000

jmp_abs:
    need LABEL, target
    emit 01001100
    reloc abs, target, 16

jmp_abs_addr:
    need REG, VALUE, SYMBOL, addr
    bitfit addr.binary, 16
    zext _addr16, addr.binary, 16
    emit 01001100
    emit _addr16

jmp_ind:
    need LABEL, target
    emit 01101100
    reloc abs, target, 16

jmp_ind_addr:
    need REG, VALUE, SYMBOL, addr
    bitfit addr.binary, 16
    zext _addr16, addr.binary, 16
    emit 01101100
    emit _addr16

jsr_abs:
    need LABEL, target
    emit 00100000
    reloc abs, target, 16

jsr_abs_addr:
    need REG, VALUE, SYMBOL, addr
    bitfit addr.binary, 16
    zext _addr16, addr.binary, 16
    emit 00100000
    emit _addr16

lda_imm:
    need REG, VALUE, SYMBOL, imm
    bitfit imm.binary, 8
    zext _imm8, imm.binary, 8
    emit 10101001
    emit _imm8

lda_zp:
    need REG, VALUE, SYMBOL, addr
    bitfit addr.binary, 8
    zext _addr8, addr.binary, 8
    emit 10100101
    emit _addr8

lda_zpx:
    need REG, VALUE, SYMBOL, addr
    need REG, x_reg
    bitfit addr.binary, 8
    zext _addr8, addr.binary, 8
    emit 10110101
    emit _addr8

lda_abs:
    need LABEL, target
    emit 10101101
    reloc abs, target, 16

lda_abs_addr:
    need REG, VALUE, SYMBOL, addr
    bitfit addr.binary, 16
    zext _addr16, addr.binary, 16
    emit 10101101
    emit _addr16

lda_absx:
    need LABEL, target
    need REG, x_reg
    emit 10111101
    reloc abs, target, 16

lda_absx_addr:
    need REG, VALUE, SYMBOL, addr
    need REG, x_reg
    bitfit addr.binary, 16
    zext _addr16, addr.binary, 16
    emit 10111101
    emit _addr16

lda_absy:
    need LABEL, target
    need REG, y_reg
    emit 10111001
    reloc abs, target, 16

lda_absy_addr:
    need REG, VALUE, SYMBOL, addr
    need REG, y_reg
    bitfit addr.binary, 16
    zext _addr16, addr.binary, 16
    emit 10111001
    emit _addr16

lda_indx:
    need REG, VALUE, SYMBOL, addr
    need REG, x_reg
    bitfit addr.binary, 8
    zext _addr8, addr.binary, 8
    emit 10100001
    emit _addr8

lda_indy:
    need REG, VALUE, SYMBOL, addr
    need REG, y_reg
    bitfit addr.binary, 8
    zext _addr8, addr.binary, 8
    emit 10110001
    emit _addr8

ldx_imm:
    need REG, VALUE, SYMBOL, imm
    bitfit imm.binary, 8
    zext _imm8, imm.binary, 8
    emit 10100010
    emit _imm8

ldx_zp:
    need REG, VALUE, SYMBOL, addr
    bitfit addr.binary, 8
    zext _addr8, addr.binary, 8
    emit 10100110
    emit _addr8

ldx_zpy:
    need REG, VALUE, SYMBOL, addr
    need REG, y_reg
    bitfit addr.binary, 8
    zext _addr8, addr.binary, 8
    emit 10110110
    emit _addr8

ldx_abs:
    need LABEL, target
    emit 10101110
    reloc abs, target, 16

ldx_abs_addr:
    need REG, VALUE, SYMBOL, addr
    bitfit addr.binary, 16
    zext _addr16, addr.binary, 16
    emit 10101110
    emit _addr16

ldx_absy:
    need LABEL, target
    need REG, y_reg
    emit 10111110
    reloc abs, target, 16

ldx_absy_addr:
    need REG, VALUE, SYMBOL, addr
    need REG, y_reg
    bitfit addr.binary, 16
    zext _addr16, addr.binary, 16
    emit 10111110
    emit _addr16

ldy_imm:
    need REG, VALUE, SYMBOL, imm
    bitfit imm.binary, 8
    zext _imm8, imm.binary, 8
    emit 10100000
    emit _imm8

ldy_zp:
    need REG, VALUE, SYMBOL, addr
    bitfit addr.binary, 8
    zext _addr8, addr.binary, 8
    emit 10100100
    emit _addr8

ldy_zpx:
    need REG, VALUE, SYMBOL, addr
    need REG, x_reg
    bitfit addr.binary, 8
    zext _addr8, addr.binary, 8
    emit 10110100
    emit _addr8

ldy_abs:
    need LABEL, target
    emit 10101100
    reloc abs, target, 16

ldy_abs_addr:
    need REG, VALUE, SYMBOL, addr
    bitfit addr.binary, 16
    zext _addr16, addr.binary, 16
    emit 10101100
    emit _addr16

ldy_absx:
    need LABEL, target
    need REG, x_reg
    emit 10111100
    reloc abs, target, 16

ldy_absx_addr:
    need REG, VALUE, SYMBOL, addr
    need REG, x_reg
    bitfit addr.binary, 16
    zext _addr16, addr.binary, 16
    emit 10111100
    emit _addr16

lsr:
    emit 01001010

lsr_acc:
    emit 01001010

lsr_zp:
    need REG, VALUE, SYMBOL, addr
    bitfit addr.binary, 8
    zext _addr8, addr.binary, 8
    emit 01000110
    emit _addr8

lsr_zpx:
    need REG, VALUE, SYMBOL, addr
    need REG, x_reg
    bitfit addr.binary, 8
    zext _addr8, addr.binary, 8
    emit 01010110
    emit _addr8

lsr_abs:
    need LABEL, target
    emit 01001110
    reloc abs, target, 16

lsr_abs_addr:
    need REG, VALUE, SYMBOL, addr
    bitfit addr.binary, 16
    zext _addr16, addr.binary, 16
    emit 01001110
    emit _addr16

lsr_absx:
    need LABEL, target
    need REG, x_reg
    emit 01011110
    reloc abs, target, 16

lsr_absx_addr:
    need REG, VALUE, SYMBOL, addr
    need REG, x_reg
    bitfit addr.binary, 16
    zext _addr16, addr.binary, 16
    emit 01011110
    emit _addr16

nop:
    emit 11101010

ora_imm:
    need REG, VALUE, SYMBOL, imm
    bitfit imm.binary, 8
    zext _imm8, imm.binary, 8
    emit 00001001
    emit _imm8

ora_zp:
    need REG, VALUE, SYMBOL, addr
    bitfit addr.binary, 8
    zext _addr8, addr.binary, 8
    emit 00000101
    emit _addr8

ora_zpx:
    need REG, VALUE, SYMBOL, addr
    need REG, x_reg
    bitfit addr.binary, 8
    zext _addr8, addr.binary, 8
    emit 00010101
    emit _addr8

ora_abs:
    need LABEL, target
    emit 00001101
    reloc abs, target, 16

ora_abs_addr:
    need REG, VALUE, SYMBOL, addr
    bitfit addr.binary, 16
    zext _addr16, addr.binary, 16
    emit 00001101
    emit _addr16

ora_absx:
    need LABEL, target
    need REG, x_reg
    emit 00011101
    reloc abs, target, 16

ora_absx_addr:
    need REG, VALUE, SYMBOL, addr
    need REG, x_reg
    bitfit addr.binary, 16
    zext _addr16, addr.binary, 16
    emit 00011101
    emit _addr16

ora_absy:
    need LABEL, target
    need REG, y_reg
    emit 00011001
    reloc abs, target, 16

ora_absy_addr:
    need REG, VALUE, SYMBOL, addr
    need REG, y_reg
    bitfit addr.binary, 16
    zext _addr16, addr.binary, 16
    emit 00011001
    emit _addr16

ora_indx:
    need REG, VALUE, SYMBOL, addr
    need REG, x_reg
    bitfit addr.binary, 8
    zext _addr8, addr.binary, 8
    emit 00000001
    emit _addr8

ora_indy:
    need REG, VALUE, SYMBOL, addr
    need REG, y_reg
    bitfit addr.binary, 8
    zext _addr8, addr.binary, 8
    emit 00010001
    emit _addr8

pha:
    emit 01001000

php:
    emit 00001000

pla:
    emit 01101000

plp:
    emit 00101000

rol:
    emit 00101010

rol_acc:
    emit 00101010

rol_zp:
    need REG, VALUE, SYMBOL, addr
    bitfit addr.binary, 8
    zext _addr8, addr.binary, 8
    emit 00100110
    emit _addr8

rol_zpx:
    need REG, VALUE, SYMBOL, addr
    need REG, x_reg
    bitfit addr.binary, 8
    zext _addr8, addr.binary, 8
    emit 00110110
    emit _addr8

rol_abs:
    need LABEL, target
    emit 00101110
    reloc abs, target, 16

rol_abs_addr:
    need REG, VALUE, SYMBOL, addr
    bitfit addr.binary, 16
    zext _addr16, addr.binary, 16
    emit 00101110
    emit _addr16

rol_absx:
    need LABEL, target
    need REG, x_reg
    emit 00111110
    reloc abs, target, 16

rol_absx_addr:
    need REG, VALUE, SYMBOL, addr
    need REG, x_reg
    bitfit addr.binary, 16
    zext _addr16, addr.binary, 16
    emit 00111110
    emit _addr16

ror:
    emit 01101010

ror_acc:
    emit 01101010

ror_zp:
    need REG, VALUE, SYMBOL, addr
    bitfit addr.binary, 8
    zext _addr8, addr.binary, 8
    emit 01100110
    emit _addr8

ror_zpx:
    need REG, VALUE, SYMBOL, addr
    need REG, x_reg
    bitfit addr.binary, 8
    zext _addr8, addr.binary, 8
    emit 01110110
    emit _addr8

ror_abs:
    need LABEL, target
    emit 01101110
    reloc abs, target, 16

ror_abs_addr:
    need REG, VALUE, SYMBOL, addr
    bitfit addr.binary, 16
    zext _addr16, addr.binary, 16
    emit 01101110
    emit _addr16

ror_absx:
    need LABEL, target
    need REG, x_reg
    emit 01111110
    reloc abs, target, 16

ror_absx_addr:
    need REG, VALUE, SYMBOL, addr
    need REG, x_reg
    bitfit addr.binary, 16
    zext _addr16, addr.binary, 16
    emit 01111110
    emit _addr16

rti:
    emit 01000000

rts:
    emit 01100000

sbc_imm:
    need REG, VALUE, SYMBOL, imm
    bitfit imm.binary, 8
    zext _imm8, imm.binary, 8
    emit 11101001
    emit _imm8

sbc_zp:
    need REG, VALUE, SYMBOL, addr
    bitfit addr.binary, 8
    zext _addr8, addr.binary, 8
    emit 11100101
    emit _addr8

sbc_zpx:
    need REG, VALUE, SYMBOL, addr
    need REG, x_reg
    bitfit addr.binary, 8
    zext _addr8, addr.binary, 8
    emit 11110101
    emit _addr8

sbc_abs:
    need LABEL, target
    emit 11101101
    reloc abs, target, 16

sbc_abs_addr:
    need REG, VALUE, SYMBOL, addr
    bitfit addr.binary, 16
    zext _addr16, addr.binary, 16
    emit 11101101
    emit _addr16

sbc_absx:
    need LABEL, target
    need REG, x_reg
    emit 11111101
    reloc abs, target, 16

sbc_absx_addr:
    need REG, VALUE, SYMBOL, addr
    need REG, x_reg
    bitfit addr.binary, 16
    zext _addr16, addr.binary, 16
    emit 11111101
    emit _addr16

sbc_absy:
    need LABEL, target
    need REG, y_reg
    emit 11111001
    reloc abs, target, 16

sbc_absy_addr:
    need REG, VALUE, SYMBOL, addr
    need REG, y_reg
    bitfit addr.binary, 16
    zext _addr16, addr.binary, 16
    emit 11111001
    emit _addr16

sbc_indx:
    need REG, VALUE, SYMBOL, addr
    need REG, x_reg
    bitfit addr.binary, 8
    zext _addr8, addr.binary, 8
    emit 11100001
    emit _addr8

sbc_indy:
    need REG, VALUE, SYMBOL, addr
    need REG, y_reg
    bitfit addr.binary, 8
    zext _addr8, addr.binary, 8
    emit 11110001
    emit _addr8

sec:
    emit 00111000

sed:
    emit 11111000

sei:
    emit 01111000

sta_zp:
    need REG, VALUE, SYMBOL, addr
    bitfit addr.binary, 8
    zext _addr8, addr.binary, 8
    emit 10000101
    emit _addr8

sta_zpx:
    need REG, VALUE, SYMBOL, addr
    need REG, x_reg
    bitfit addr.binary, 8
    zext _addr8, addr.binary, 8
    emit 10010101
    emit _addr8

sta_abs:
    need LABEL, target
    emit 10001101
    reloc abs, target, 16

sta_abs_addr:
    need REG, VALUE, SYMBOL, addr
    bitfit addr.binary, 16
    zext _addr16, addr.binary, 16
    emit 10001101
    emit _addr16

sta_absx:
    need LABEL, target
    need REG, x_reg
    emit 10011101
    reloc abs, target, 16

sta_absx_addr:
    need REG, VALUE, SYMBOL, addr
    need REG, x_reg
    bitfit addr.binary, 16
    zext _addr16, addr.binary, 16
    emit 10011101
    emit _addr16

sta_absy:
    need LABEL, target
    need REG, y_reg
    emit 10011001
    reloc abs, target, 16

sta_absy_addr:
    need REG, VALUE, SYMBOL, addr
    need REG, y_reg
    bitfit addr.binary, 16
    zext _addr16, addr.binary, 16
    emit 10011001
    emit _addr16

sta_indx:
    need REG, VALUE, SYMBOL, addr
    need REG, x_reg
    bitfit addr.binary, 8
    zext _addr8, addr.binary, 8
    emit 10000001
    emit _addr8

sta_indy:
    need REG, VALUE, SYMBOL, addr
    need REG, y_reg
    bitfit addr.binary, 8
    zext _addr8, addr.binary, 8
    emit 10010001
    emit _addr8

stx_zp:
    need REG, VALUE, SYMBOL, addr
    bitfit addr.binary, 8
    zext _addr8, addr.binary, 8
    emit 10000110
    emit _addr8

stx_zpy:
    need REG, VALUE, SYMBOL, addr
    need REG, y_reg
    bitfit addr.binary, 8
    zext _addr8, addr.binary, 8
    emit 10010110
    emit _addr8

stx_abs:
    need LABEL, target
    emit 10001110
    reloc abs, target, 16

stx_abs_addr:
    need REG, VALUE, SYMBOL, addr
    bitfit addr.binary, 16
    zext _addr16, addr.binary, 16
    emit 10001110
    emit _addr16

sty_zp:
    need REG, VALUE, SYMBOL, addr
    bitfit addr.binary, 8
    zext _addr8, addr.binary, 8
    emit 10000100
    emit _addr8

sty_zpx:
    need REG, VALUE, SYMBOL, addr
    need REG, x_reg
    bitfit addr.binary, 8
    zext _addr8, addr.binary, 8
    emit 10010100
    emit _addr8

sty_abs:
    need LABEL, target
    emit 10001100
    reloc abs, target, 16

sty_abs_addr:
    need REG, VALUE, SYMBOL, addr
    bitfit addr.binary, 16
    zext _addr16, addr.binary, 16
    emit 10001100
    emit _addr16

tax:
    emit 10101010

tay:
    emit 10101000

tsx:
    emit 10111010

txa:
    emit 10001010

txs:
    emit 10011010

tya:
    emit 10011000

rompad_to_vectors:
    atari_pad_to 4090

vectors:
    need LABEL, target
    atari_vectors target
